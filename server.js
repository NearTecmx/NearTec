import http from 'http';import fs from 'fs';import path from 'path';import url from 'url';
const root=process.cwd();const release='NEARTEC-FINAL-RENDER-LIGHT-TRUE-SCENE-2026';const types={'.html':'text/html;charset=utf-8','.css':'text/css','.js':'application/javascript','.png':'image/png','.webp':'image/webp','.svg':'image/svg+xml','.json':'application/json'};
const server=http.createServer((req,res)=>{let p=url.parse(req.url).pathname;if(p==='/api/health'){res.writeHead(200,{'content-type':'application/json','cache-control':'no-store'});res.end(JSON.stringify({ok:true,service:'NearTec Site API',status:'healthy',release,time:new Date().toISOString()}));return}if(p.startsWith('/api/')){res.writeHead(200,{'content-type':'application/json','cache-control':'no-store'});res.end(JSON.stringify({ok:true,local:true,release}));return}let file=path.join(root,p);if(p.endsWith('/'))file=path.join(root,p,'index.html');if(!fs.existsSync(file)&&!path.extname(file))file=path.join(root,p,'index.html');if(!fs.existsSync(file))file=path.join(root,'404.html');const ext=path.extname(file);res.writeHead(200,{'content-type':types[ext]||'application/octet-stream','cache-control':ext==='.html'?'no-store':'public, max-age=0, must-revalidate'});fs.createReadStream(file).pipe(res)});server.listen(process.env.PORT||3000,()=>console.log('NearTec local server on '+(process.env.PORT||3000)));
